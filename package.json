{
  "name": "note-taking-fly",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "standard": "^17.1.0",
    "vite": "^5.2.0"
  },
  "dependencies": {
    "js-base64": "^3.7.7",
    "marked": "^13.0.0"
  },
  "eslintConfig": {
    "extends": [
      "./node_modules/standard/eslintrc.json"
    ],
    "rules": {
      "no-unused-vars": "warn",
      "comma-dangle": "off"
    }
  }
}
